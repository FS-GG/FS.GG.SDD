# Plan: Physical Candidate-Inventory Omission Characterization

1. Pin only the selected spec in a disposable tree with an omitted physical duplicate; show #1026's pure success.
2. Use the existing Linux closed-root capture as an independent negative control for the omitted path, then supply both captures to #1026 and require duplicate-ID refusal.
3. Repeat with a candidate inserted after pathname enumeration to characterize stale proposals; require an unrelated candidate to pass when the declared set is complete.
4. Run focused and full Commands tests plus a warning-clean Release build, then open a stacked draft with the remaining physical-discovery policy explicit.

This slice adds no output effect or publication path.
