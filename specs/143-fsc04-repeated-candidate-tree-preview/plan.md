# Plan: Repeated Complete Candidate Tree Preview

1. Reproduce #1028's late duplicate candidate with a disposable, ordered two-child `work/` tree; require the first pass to succeed and the second to refuse.
2. Add a read-only adapter that repeats the complete pinned capture and pure inventory verification with the same declared set, then compares captured paths and raw bytes.
3. Test stable, changed-byte, changed-roster and linked-candidate controls independently.
4. Run focused and full Commands tests plus warning-clean Release build, then publish an exact-head stacked draft for review without any output effect.
