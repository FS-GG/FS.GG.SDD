# Plan

1. Reproduce the mode-only false green against both Git regular-file modes in disposable repositories.
2. Carry Linux opened-descriptor mode with selected bytes, checking mode stability during the existing two byte passes.
3. Compare the captured executable policy with the committed Git mode in a separate observed-only preview.
4. Run focused and full Commands tests and a Release warnings-as-errors build, then submit a stacked draft with explicit custody and effect holds.
