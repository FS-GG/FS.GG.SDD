# Plan

1. Red-before characterize valid foreign root and `.fsgg` child-tree payloads under the selected IDs in SHA-1 and SHA-256 disposable repositories; distinguish the child false green from Git's existing root refusal.
2. Parse the root-tree ID from verified commit bytes; verify bounded root and child tree bodies against their object IDs and select core file entries from held tree bytes.
3. Run focused tree controls, the broader Git preview set, full Commands tests and a Release warnings-as-errors build.
4. Submit a non-authorizing stacked draft with physical handles, ABA, common instant and producer authority left open.
