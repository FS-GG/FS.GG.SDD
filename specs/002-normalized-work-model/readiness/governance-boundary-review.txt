Governance boundary review
==========================

Result: PASS

The normalized work model records optional Governance boundary paths from the
SDD-owned .fsgg/project.yml source:

- .fsgg/capabilities.yml
- .fsgg/policy.yml
- .fsgg/tooling.yml

These entries are emitted as optionalCompatibilityBoundary facts with owner
governance and requiredBySdd=false. The implementation does not parse
Governance policy, select routes, adjust severity by profile, compute evidence
freshness, select gates, emit audit verdicts, or enforce protected boundaries.

Verification:

- GovernanceBoundaryTests
- NormalizedWorkModelTests
- FSI prelude output in fsi-session.txt
