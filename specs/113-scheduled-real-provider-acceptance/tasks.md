# Tasks — Reliable scheduled real-provider acceptance

- [x] Add a red offline contract test for the scheduled lane and local capability skip.
- [x] Remove the scheduled neutral-skip branch while preserving registry source precedence.
- [x] Provision the repository's read-only real-provider registry secret.
- [x] Make the default real-provider product root deterministically identifier-safe.
- [x] Isolate the default build probe from persistent build-server pipe inheritance.
- [x] Run acceptance, workflow-contract, formatting, and Release solution gates.
- [x] Define a manual no-input workflow dispatch of the merged workflow as the completion obligation.
