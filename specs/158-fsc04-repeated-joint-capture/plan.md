# Plan

1. Show a single joined observation succeeds after a source changes immediately after capture.
2. Require two individually valid bundle/tree pairs with identical rosters and raw bytes across passes.
3. Exercise config, selected work and candidate-roster mutations, then run focused and full Commands tests and a warnings-as-errors build.
