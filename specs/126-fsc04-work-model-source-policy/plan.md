# Plan

Use public currency checking with fixture snapshots and manipulated generated-source metadata to isolate current source comparison. Exercise `ViewGeneration.workModelSnapshots` directly for its evidence projection. Keep production source and output code unchanged until the producer owner selects a multi-root closure and digest policy.
