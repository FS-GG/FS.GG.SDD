# Plan: Pinned Relative-Path Length Cap

1. Characterize #1034 with disposable relative paths of 1,024 and 1,025 characters, built from valid filesystem segments; require the longer path to refuse.
2. Check closed-root and declared paths before retained-set construction, discovered paths before roster storage and selected-file paths before traversal.
3. Run focused path-cap controls and the full Commands suite plus warning-clean Release build.
4. Open an exact-head stacked draft without output effects or installed-parity claims.
