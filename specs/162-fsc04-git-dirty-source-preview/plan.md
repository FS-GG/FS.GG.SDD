# Plan

1. Reproduce the commit-only false green for changed project bytes, changed sdd bytes, and a symlink to equal bytes in a disposable repository.
2. Compare both selected working-tree files to the named commit blobs with the existing pinned no-follow reader; refuse differences and physical failures.
3. Keep clean, unselected-file, and mode-only scope controls; run focused and full Commands tests and a Release warnings-as-errors build.
4. Submit a stacked draft with an observed-only result and explicit temporal, mode, source-selection, and installed-parity limits.
