# Plan

1. Add a test-only interleaving seam after each pinned selected-file read, keeping the production wrapper unchanged.
2. Construct repeated physical bundle captures whose selected project/sdd pair never existed together, and a persistent-change refusal control.
3. Run focused and full Commands tests and a Release warnings-as-errors build; open a stacked characterization draft.
