# Plan

1. Show separately verified bundle and work-tree captures can be green with different selected `work/` bytes.
2. Re-verify both captured sets and require exact raw-byte equality on every overlapping selected work path.
3. Run focused and full Commands tests and a Release warnings-as-errors build; open a stacked read-only draft.
