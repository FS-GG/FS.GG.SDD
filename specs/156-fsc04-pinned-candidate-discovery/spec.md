# Pinned Complete Candidate Discovery Preview

**Status:** read-only FSC-04 candidate-inventory prerequisite, stacked on draft #1041. **Owner:** FS.GG.SDD.

## Red-before and repair

The pure candidate inventory interpreter accepts a caller-supplied selected-only list even when `work/a/spec.md` physically declares the selected `work/z` ID. That independent false-green control is repeated in this slice. Earlier declared closed-root captures refuse the omitted file, but require the caller to name every file first, leaving candidate-list authority outside the adapter.

The Linux pinned complete-root reader now has a discovery mode: it walks every entry under the held `work/` descriptor without a caller file declaration, retaining the existing no-follow, alias, type, directory-stability and resource refusals. The existing repeated candidate preview uses two discovered captures, interprets each complete captured inventory, and refuses differing rosters or raw bytes. It has no source/output write path. Tests show the omitted duplicate is refused, an unrelated candidate succeeds without a supplied list, a missing selected spec is refused, a late child addition is refused, and a linked candidate is refused.

## Boundary

This removes caller file-list authority for this **preview of the single `work/` root**. It does not select or bind `.fsgg`, performance artifacts, or generation inputs, and strict whole-root treatment of unrelated links and special entries still needs producer acceptance. Two passes and held-descriptor checks do not prove an atomic tree or cross-root instant: ABA, timestamp-hidden/post-check changes and adversarial schedules remain open. #1040 Unicode policy and pinned reader limits are provisional; Windows parity, #1017 physical custody, #1018 verification/staging/rollback, installed parity, publication, receiver adoption, merge and GS2-10 freeze remain held. No live effect or output is produced.
