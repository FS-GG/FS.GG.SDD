# Plan

1. Reproduce the selected-only pure inventory false green while a duplicate work-ID candidate is physically present.
2. Allow the pinned whole-root traversal to discover files without caller declarations, retaining declared mode and all physical guards.
3. Feed two discovered captures to the existing read-only inventory preview, then run focused and full Commands tests and a warnings-as-errors build.
