# Plan

1. Reproduce the nested copied-workspace false green with a disposable Git repository.
2. Refuse a supplied root that Git reports as nested or bare before any selected commit reads.
3. Preserve real repository-root and linked-worktree-root controls.
4. Run focused and full Commands tests and a Release warnings-as-errors build; submit an observed-only stacked draft.
