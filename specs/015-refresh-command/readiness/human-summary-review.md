# Human Summary Review — `readiness/<id>/summary.md`

Captured from a real CLI run (`fsgg-sdd refresh --root <tmp> --work 015-refresh-command`)
against a disposable shipped project. The rendered file is
[`sample-summary.md`](sample-summary.md).

Review findings:

- **Generated marker present.** The file opens with
  `<!-- GENERATED by fsgg-sdd refresh — DO NOT EDIT ... -->` recording the view
  kind (`summary`), schema version (1), generator identity, every structured
  readiness source with its SHA-256 digest and schema status, and the output
  digest. A human is clearly warned not to hand-edit it (FR-007, Constitution VII).
- **Projection only.** Every fact in the body (lifecycle stage, outcome,
  disposition, the per-view currency table, the diagnostics list, and the next
  action) is also present in the authoritative JSON report. The summary
  introduces no independent fact (FR-006, SC-008); the artifact-level test
  `RefreshSummaryViewTests` and the command-level test
  `refresh summary per-view table matches the report per-view state` assert this.
- **Deterministic.** No clocks, durations, ANSI, terminal width, host path
  separators, randomness, or absolute host paths appear. The command-level test
  `refresh produces a byte-identical summary across repeated runs` confirms
  byte-identical output for identical inputs.
- **Honest blocking.** When a structured input is missing/stale/blocked the
  summary is not written from unusable data; the report records
  `refresh.unrenderableSummary` and the summary view is `blocked`
  (test: `refresh blocks the summary and records a diagnostic when structured
  inputs are unusable`).
