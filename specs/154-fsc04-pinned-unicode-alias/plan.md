# Plan

1. Show the accepted #1039 Linux reader accepts both declared canonical-equivalent files and accepts a selected file beside its alias.
2. Refuse canonical and case aliases at pinned declaration, physical roster and selected-name checks, preserving exact paths and bytes.
3. Run focused and full Commands tests plus Release warnings-as-errors build; publish only a stacked draft for review.
