# Plan

1. Use a disposable hook after the initial registration check to switch `.git` from repository A to sibling B, then show B's selected commit is accepted before repair.
2. Repeat top-level and registration checks after selected blob reads and refuse a persistent switch.
3. Preserve a stable-repository control and a swap-back ABA characterization.
4. Run focused and full Commands tests and a Release warnings-as-errors build; submit an observed-only stacked draft.
