# Plan

1. Demonstrate an equal-length loose-blob payload substitution under the selected Git object ID, with SHA-1 and SHA-256 repositories and each selected core path.
2. Add an independent Git blob-ID digest refusal over the actual returned bytes.
3. Keep clean-format controls and run focused/full Commands tests and a Release warnings-as-errors build.
4. Submit a source-only, non-authorizing stacked draft with the remaining commit/tree, handle, ABA and common-instant limits explicit.
