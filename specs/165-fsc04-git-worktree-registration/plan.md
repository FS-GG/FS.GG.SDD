# Plan

1. Reproduce false greens from a symlinked `.git` entry and a forged regular gitfile in disposable copied workspaces.
2. Require exact supplied-root registration in Git's bounded NUL-delimited worktree roster before commit reads.
3. Preserve conventional and linked-worktree controls; run focused and full Commands tests and a Release warnings-as-errors build.
4. Submit a stacked observed-only draft with the mutable-registry and cross-command custody limits explicit.
