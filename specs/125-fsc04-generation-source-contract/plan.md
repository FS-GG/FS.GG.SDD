# Plan

Add an internal versioned verifier after `GenerationSourceSnapshot`. Keep the producer selection separate from the candidate manifest. Exercise positive and negative cases in an isolated temporary tree. Do not wire the verifier into work-model production until its scattered source set and evidence transform are resolved by the producer owner.
