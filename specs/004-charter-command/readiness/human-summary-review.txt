Review: PASS

The charter text projection exposes the SC-006 review facts from the same `CommandReport` object as JSON:

- `command: charter`
- `outcome: ...`
- `changedArtifacts: ...`
- `generatedViews: ...`
- `diagnostics: ...`
- `nextAction: nextLifecycleCommand`

The CLI smoke transcript confirms a maintainer can identify changed artifact count, generated-view count, blocking diagnostic count, and next action from text output without extra facts outside the authoritative report.
