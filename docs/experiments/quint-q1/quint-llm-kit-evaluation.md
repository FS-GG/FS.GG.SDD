# Pinned Quint LLM Kit evaluation

Candidate: `quint-co/quint-llm-kit@cc75369f741af7d490936f82002c2d28e3b3d78d`

License: Apache-2.0, license SHA-256 `5cc84061e5937535827c4fd3446c7609ad87065b55733b1874b2ddc67df04bf0`
Tracked-tree receipt: `68a11d403846de3af26759eef97f4a35eff5e71d561d41ea17d96e535c171556`

The corpus is guidance, never semantic or installation authority. No installer, Docker image, moving
branch, MCP server, or downloaded plugin was executed during Q1.

| Surface | Disposition | Evidence and reason |
|---|---|---|
| `quint-lang` skill (`ba2312…`) | Adapt | Its type/action/test/simulation reference was used against Quint 0.32.0; FS-GG removes moving install advice and binds the profile/tool digest. |
| `quint-modeling` skill (`fe595b…`) | Adapt | Adopt executable-first decomposition, pure transition logic, explicit action witnesses, and separate tests; require compiled-contract IDs and evidence relationships the upstream skill does not own. |
| `quint-execute-spec` skill (`7bd1db…`) | Adapt | Adopt spec→plan→implementation→correspondence separation; keep product adapters in consumers and prohibit silent semantic edits. |
| Typecheck before run | Adopt | Fast, deterministic structural feedback; always selected for semantic source. |
| Sampled `quint run` inner loop | Adopt | Seeded Rust runs are fast and yield ITF/MBT inputs; they never substitute for model checking. |
| Reachability witnesses | Adopt | Each slice names a state whose reachability prevents vacuous safety. |
| Trace explanation / first divergent prefix | Adopt | Required for mutations and S.I.R. replay; raw terminal output alone is insufficient evidence. |
| Transition labels | Adapt | Use stable catalogue action IDs rather than compiler node IDs or implementation method names. |
| Type/listener coverage | Adapt | Type/action catalogue coverage applies to plain Quint; listener coverage is selected only for Choreo models. |
| Choreo default for message protocols | Reject for Q1 coordination | The slice is a shared-authority state machine, not peer messaging. Choreo adds framework state and supply-chain cost without improving the demonstrated contract. |
| Docker/Claude/MCP environment | Reject | Runtime-specific, large closure, and installs moving/latest tools. The three standalone skills are sufficient guidance input. |
| `curl .../main | bash`, plugin install, latest Quint | Reject | Moving network content cannot satisfy a reproducible qualification or permission boundary. |
| `/code:*` and `/refactor:*` orchestration | Defer to Q2/Q3 | Useful after a backend exists; Q1 changes no implementation/public authority. |

The upstream disclaimer that the kit is internally developed and not thoroughly evaluated remains an
explicit risk. Q1's controls therefore test tool behavior and exact source bytes; they do not treat a
prompt or agent response as a pass.
