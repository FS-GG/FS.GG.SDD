# 055 (FR-009 / research R3): a provider that writes into the reserved fs-gg-sdd-*
# skill namespace under .claude/skills/ and .codex/skills/ is a provider defect. Re-pointed
# from the (now-legitimate) whole-root `leak` shape to a NEW reserved-namespace path
# (.{claude,codex}/skills/fs-gg-sdd-custom/) — a name never seeded, so the write survives
# the skeletonFiles subtraction and reaches the narrowed isSddTree guard. This fixture
# template declares the `lifecycle` symbol (so `dotnet new` accepts the forwarded param).
schemaVersion: 1
providers:
  - name: fixture
    contractVersion: "1.0.0"
    templateId: fsgg-fixture-skills-intrusion
    source: __FIXTURE__/skills-intrusion
    parameters:
      - key: lifecycle
        required: false
