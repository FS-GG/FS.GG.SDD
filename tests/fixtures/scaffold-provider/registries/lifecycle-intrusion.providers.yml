# New fixture (not a reuse of `writes-into-fsgg/`): `dotnet new` rejects an
# undeclared `-p:lifecycle`, so the SDD-tree-intrusion edge needs a template that
# declares the `lifecycle` symbol and still writes into .fsgg/work/readiness. See
# tasks T008.
schemaVersion: 1
providers:
  - name: fixture
    contractVersion: "1.0.0"
    templateId: fsgg-fixture-lifecycle-intrusion
    source: __FIXTURE__/lifecycle-intrusion
    parameters:
      - key: lifecycle
        required: false
