A co-tenant provider skill outside the reserved fs-gg-sdd-* namespace. A compliant
provider may populate the shared .claude/skills/ root with its own product skills;
this one is recorded as generatedProduct, not an SDD-tree intrusion (feature 055).
