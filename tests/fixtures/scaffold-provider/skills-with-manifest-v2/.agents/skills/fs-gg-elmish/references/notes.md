<!-- SYNTHETIC: an auxiliary file of the fixture co-tenant skill, so this provider's ADR-0017
     v2 row declares a file set with MORE than SKILL.md in it. FS.GG.SDD#739: an amend that
     dropped `files` was invisible while every row happened to be one file long. -->
# fs-gg-elmish notes (fixture)

Auxiliary content. Its only job is to be a second declared file whose digest a faithful
`parse -> amend -> serialize` round-trip must carry through unchanged.
