{
  "name": "polyglot-client-acceptance",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "tsc --project tsconfig.json",
    "test": "npm run compile && node --test --test-reporter=junit dist/client.test.js > ../results/client.junit.xml"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3"
  }
}
