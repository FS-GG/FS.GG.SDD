schemaVersion: 1
fixture:
  id: specify-adds-missing-sections
  workId: 005-specify-command
  purpose: Rerunning specify appends missing standard sections without rewriting existing prose.
expected:
  command: specify
  outcome: succeeded
  operation: update
  safeWriteDecision: safe

