schemaVersion: 1
fixture:
  id: text-projection
  workId: 004-charter-command
  purpose: Charter plain text is a projection of command report facts.
expected:
  command: charter
  fields: [command, outcome, changedArtifacts, generatedViews, diagnostics, nextAction]
scenarios:
  - command: specify
    fields:
      - command
      - outcome
      - changedArtifacts
      - specificationRequirements
      - unresolvedAmbiguities
      - generatedViews
      - diagnostics
      - nextAction
  - command: clarify
    fields:
      - command
      - outcome
      - changedArtifacts
      - clarificationQuestions
      - clarificationDecisions
      - acceptedDeferrals
      - remainingAmbiguities
      - generatedViews
      - diagnostics
      - nextAction
  - command: plan
    fields:
      - command
      - outcome
      - changedArtifacts
      - planDecisions
      - planContractReferences
      - planVerificationObligations
      - planAcceptedDeferrals
      - planStaleDecisions
      - generatedViews
      - diagnostics
      - nextAction
