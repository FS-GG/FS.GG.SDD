schemaVersion: 1
fixture:
  id: text-projection
  workId: 004-charter-command
  purpose: Charter plain text is a projection of command report facts.
expected:
  command: charter
  fields: [command, outcome, changedArtifacts, generatedViews, diagnostics, nextAction]
